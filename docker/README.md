# Build with args

```bash
docker build -f Dockerfile --build-arg GIT_URL=https://github.com/tuxmonteiro/GloboDNS.git --build-arg GIT_BRANCH=wip2  -t docker.artifactory.globoi.com/ateam/globodns:20190214 .
```
